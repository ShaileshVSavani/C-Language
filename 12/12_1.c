/*Creat array of structure and creat grading system which implements following concepts:
 -user can create N number of students.
 -Add auto calculation of perentage and marks.
 -Print all the records of N students in tabuler formate.
 Add searching functionality that user can search anystudent bt id.*/
 
 #include<stdio.h>
 struct student{
 	int id;
 	char name;
 	float per;
 };
void main(){
	int n,i,id,count=0;
	printf("Enter number of students:");
	scanf("%d",&n);
	
	struct student s[n];
	
	for(i=0;i<n;i++){
		printf("\nEnter id\t:");
		scanf("%d",&s[i].id);
		
		fflush(stdin);
		
		printf("Enter name\t:");
		scanf("%s",&s[i].name);
		printf("Enter per\t:");
		scanf("%f",&s[i].per);	
	}
/*	printf("\n\n\nId\tName\tPer\n=====  =====  ======\n");
	for(i=0;i<n;i++){
	printf("%d\t%s\t%.2f\n",s[i].id,s[i].name,s[i].per);	
	}
*/	
	printf("\nEnter Id for Search:");
	scanf("%d",&id);
	
	for(i=0;i<n;i++){
		if(id==s[i].id){
				printf("\nId\tName\tPer\n=====  =====  =====\n");
				printf("%d\t%s\t%.2f\n",s[i].id,s[i].name,s[i].per);
				count++;
		}
		if(count==0){
			printf("\nId not found.");
		}
	}
}





















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































 
